{
  "name": "cap-todo",
  "description": "Generated by cds init",
  "repository": "<Add your repository here>",
  "license": "ISC",
  "version": "1.0.0",
  "engines": {
    "node": "^8.9"
  },
  "files": [],
  "dependencies": {
    "@sap/cds": "^3.16.2",
    "@sap/cds-odata-v2-adapter-proxy": "^1.2.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "sqlite3": "^4.1.0"
  },
  "scripts": {
    "start": "cds run",
    "build": "cds build/all --clean",
    "deploy": "cds deploy",
    "v2server": "node v2server"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv"
        ],
        "credentials": {
          "database": "db/todo.db"
        }
      }
    }
  }
}
